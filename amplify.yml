version: 1.0
env:
  variables:
      VERSION_HUGO: 0.55.6
frontend:
  phases:
    build:
      commands:
        - env
        - hugo -v
  artifacts:
    baseDirectory: public
    files:
      - '**/*'
  cache:
    paths: []

